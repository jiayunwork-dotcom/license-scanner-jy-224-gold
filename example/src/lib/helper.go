package lib

// Helper is intentionally free of any license declaration, so the scanner
// treats it as a source file with no recognized license and skips it
// (it does not appear in the findings).
func Helper() string { return "no license header here" }
